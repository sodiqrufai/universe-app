// Single source of truth for the backend base URL — mirrors
// universe_mobile/lib/config/api_config.dart exactly, so mobile and
// admin never drift onto different backends.
export const API_BASE_URL = 'https://universe-app-w4cc.onrender.com';